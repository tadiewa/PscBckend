spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsallowance
    username: postgres
    password: tadiewa
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  mvc:
    problemdetails:
      enabled: true

server:
  port: 9076

# app:
#   cors:
#     allowed-origins: "http://localhost:5173"
